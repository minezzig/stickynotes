{
  "name": "server",
  "version": "1.0.0",
  "description": "backend for stickyNotes app",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "dev": "node --watch server.js",
    "db:generate": "drizzle-kit generate:pg --config drizzle.config.js",
    "db:push": "drizzle-kit push:pg --config drizzle.config.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@neondatabase/serverless": "^0.9.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "drizzle-orm": "^0.30.1",
    "express": "^4.18.3",
    "pg": "^8.11.3"
  },
  "devDependencies": {
    "drizzle-kit": "^0.20.14"
  }
}
